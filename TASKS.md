# Tasks

## Next

- After the user completes `npm login`, re-run `npm whoami`, then continue the approved rc.1 publish flow with `npm publish --tag next` and the prepared GitHub Pre-release.
- Remove the already verified `.p7-install-temp` directory only with an approved safe cleanup action.

## Out of scope

- No tag, npm publication, GitHub Release, commit, push, or pull request without explicit user direction.
- Do not add execution, sandboxing, or real agent control in P6.
